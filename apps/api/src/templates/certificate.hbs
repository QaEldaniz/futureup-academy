<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&family=Space+Grotesk:wght@500;600;700&display=swap');

    * { margin: 0; padding: 0; box-sizing: border-box; }

    body {
      width: 297mm;
      height: 210mm;
      font-family: 'Inter', sans-serif;
      background: #ffffff;
      position: relative;
      overflow: hidden;
    }

    .certificate {
      width: 100%;
      height: 100%;
      position: relative;
      background: #ffffff;
    }

    /* Decorative gradient border */
    .border-frame {
      position: absolute;
      inset: 0;
      border: 3.5mm solid transparent;
      background-image:
        linear-gradient(white, white),
        linear-gradient(135deg, #6C3CE1, #1E40AF, #06B6D4, #1E40AF, #6C3CE1);
      background-origin: border-box;
      background-clip: padding-box, border-box;
      pointer-events: none;
    }

    /* Corner decorations */
    .corner {
      position: absolute;
      width: 30mm;
      height: 30mm;
      opacity: 0.06;
    }
    .corner-tl { top: 8mm; left: 8mm; background: radial-gradient(circle at top left, #6C3CE1, transparent 70%); }
    .corner-tr { top: 8mm; right: 8mm; background: radial-gradient(circle at top right, #06B6D4, transparent 70%); }
    .corner-bl { bottom: 8mm; left: 8mm; background: radial-gradient(circle at bottom left, #06B6D4, transparent 70%); }
    .corner-br { bottom: 8mm; right: 8mm; background: radial-gradient(circle at bottom right, #6C3CE1, transparent 70%); }

    /* Content container */
    .content {
      position: relative;
      z-index: 1;
      padding: 18mm 25mm;
      height: 100%;
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    /* Header */
    .header {
      text-align: center;
      margin-bottom: 4mm;
    }

    .header-bar {
      width: 80mm;
      height: 1.5mm;
      background: linear-gradient(90deg, transparent, #6C3CE1, #1E40AF, #06B6D4, transparent);
      border-radius: 1mm;
      margin: 0 auto 5mm;
    }

    .academy-name {
      font-family: 'Space Grotesk', sans-serif;
      font-size: 13pt;
      font-weight: 700;
      color: #6C3CE1;
      letter-spacing: 4px;
      text-transform: uppercase;
    }

    .certificate-title {
      font-family: 'Space Grotesk', sans-serif;
      font-size: 30pt;
      font-weight: 700;
      background: linear-gradient(135deg, #6C3CE1, #1E40AF);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      margin-top: 2mm;
      line-height: 1.2;
    }

    .subtitle {
      font-size: 10pt;
      color: #6b7280;
      margin-top: 3mm;
      font-weight: 400;
    }

    /* Divider line */
    .divider {
      width: 40mm;
      height: 0.5mm;
      background: linear-gradient(90deg, transparent, #d1d5db, transparent);
      margin: 4mm auto;
    }

    /* Student name */
    .student-name {
      font-family: 'Space Grotesk', sans-serif;
      font-size: 28pt;
      font-weight: 700;
      color: #111827;
      margin: 3mm 0;
      text-align: center;
      line-height: 1.2;
    }

    /* Course details */
    .course-label {
      font-size: 10pt;
      color: #6b7280;
      margin-top: 2mm;
    }

    .course-name {
      font-size: 15pt;
      font-weight: 600;
      color: #1E40AF;
      margin-top: 1mm;
      text-align: center;
    }

    /* Meta info row */
    .meta-row {
      display: flex;
      justify-content: center;
      gap: 20mm;
      margin-top: 5mm;
    }

    .meta-item {
      text-align: center;
    }

    .meta-label {
      font-size: 7pt;
      color: #9ca3af;
      text-transform: uppercase;
      letter-spacing: 1.5px;
      font-weight: 600;
    }

    .meta-value {
      font-size: 10pt;
      color: #374151;
      font-weight: 600;
      margin-top: 1mm;
    }

    .grade-badge {
      display: inline-block;
      padding: 1mm 4mm;
      background: linear-gradient(135deg, #6C3CE1, #1E40AF);
      color: white;
      border-radius: 3mm;
      font-size: 9pt;
      font-weight: 600;
      margin-top: 1mm;
    }

    /* Teacher review */
    .review-section {
      margin-top: 4mm;
      padding: 3mm 6mm;
      background: #f8f6ff;
      border-left: 2.5px solid #6C3CE1;
      border-radius: 0 2mm 2mm 0;
      max-width: 220mm;
      width: 100%;
    }

    .review-text {
      font-size: 8.5pt;
      color: #4b5563;
      font-style: italic;
      line-height: 1.5;
    }

    /* Footer */
    .footer {
      margin-top: auto;
      width: 100%;
      display: flex;
      justify-content: space-between;
      align-items: flex-end;
    }

    .signature-block {
      text-align: center;
    }

    .signature-line {
      width: 50mm;
      height: 0.3mm;
      background: #d1d5db;
      margin-bottom: 2mm;
    }

    .signature-name {
      font-size: 10pt;
      font-weight: 600;
      color: #374151;
    }

    .signature-role {
      font-size: 7.5pt;
      color: #9ca3af;
      margin-top: 0.5mm;
    }

    .qr-block {
      text-align: center;
    }

    .qr-code img {
      width: 22mm;
      height: 22mm;
      border: 0.5mm solid #e5e7eb;
      border-radius: 2mm;
      padding: 1mm;
    }

    .cert-code {
      font-family: 'Space Grotesk', monospace;
      font-size: 7pt;
      color: #9ca3af;
      margin-top: 1mm;
      letter-spacing: 0.5px;
    }

    .verify-text {
      font-size: 6pt;
      color: #d1d5db;
      margin-top: 0.5mm;
    }

    /* Bottom gradient bar */
    .bottom-bar {
      position: absolute;
      bottom: 10mm;
      left: 25mm;
      right: 25mm;
      height: 1mm;
      background: linear-gradient(90deg, transparent, #6C3CE1, #1E40AF, #06B6D4, transparent);
      border-radius: 0.5mm;
    }
  </style>
</head>
<body>
  <div class="certificate">
    <div class="border-frame"></div>
    <div class="corner corner-tl"></div>
    <div class="corner corner-tr"></div>
    <div class="corner corner-bl"></div>
    <div class="corner corner-br"></div>

    <div class="content">
      <!-- Header -->
      <div class="header">
        <div class="header-bar"></div>
        <div class="academy-name">FutureUp Academy</div>
        <div class="certificate-title">Certificate of Completion</div>
        <div class="subtitle">This is to certify that</div>
      </div>

      <div class="divider"></div>

      <!-- Student Name -->
      <div class="student-name">{{studentName}}</div>

      <div class="divider"></div>

      <!-- Course -->
      <div class="course-label">has successfully completed the course</div>
      <div class="course-name">{{courseName}}</div>

      <!-- Meta Row -->
      <div class="meta-row">
        <div class="meta-item">
          <div class="meta-label">Date</div>
          <div class="meta-value">{{issueDate}}</div>
        </div>
        {{#if grade}}
        <div class="meta-item">
          <div class="meta-label">Grade</div>
          <div class="grade-badge">{{grade}}</div>
        </div>
        {{/if}}
        <div class="meta-item">
          <div class="meta-label">Instructor</div>
          <div class="meta-value">{{teacherName}}</div>
        </div>
      </div>

      <!-- Teacher Review -->
      {{#if teacherReview}}
      <div class="review-section">
        <div class="review-text">&ldquo;{{teacherReview}}&rdquo;</div>
      </div>
      {{/if}}

      <!-- Footer -->
      <div class="footer">
        <div class="signature-block">
          <div class="signature-line"></div>
          <div class="signature-name">{{teacherName}}</div>
          <div class="signature-role">Course Instructor</div>
        </div>

        <div class="signature-block">
          <div class="signature-line"></div>
          <div class="signature-name">FutureUp Academy</div>
          <div class="signature-role">Education Director</div>
        </div>

        <div class="qr-block">
          <div class="qr-code">
            <img src="{{qrCodeDataUrl}}" alt="QR Code" />
          </div>
          <div class="cert-code">{{uniqueCode}}</div>
          <div class="verify-text">Scan to verify</div>
        </div>
      </div>
    </div>

    <div class="bottom-bar"></div>
  </div>
</body>
</html>
